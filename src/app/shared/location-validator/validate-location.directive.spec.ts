import { ValidateLocationDirective } from './validate-location.directive';

describe('ValidateLocationDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateLocationDirective();
    expect(directive).toBeTruthy();
  });
});
